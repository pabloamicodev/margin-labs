query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        totalAmount {
          amount
        }
      }
      merchandise {
        ... on ProductVariant {
          __typename
          product {
            id
            metafield(namespace: "custom", key: "volume_discount_tiers") {
              jsonValue
            }
          }
        }
      }
    }
  }
}
