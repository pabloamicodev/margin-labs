query RunInput {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
    attribute {
      key
      value
    }
  }
  discountNode {
    metafield(namespace: "$app:marginlab", key: "shipping-discount-config") {
      value
    }
  }
}
